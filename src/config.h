#pragma once

// How long each prompt stays on screen before rotating to the next one.
#define PROMPT_INTERVAL_MS  20000

// How long the bulldog "acts out" the prompt (wave, tongue out, etc.)
// before settling back to idle blinking.
#define ACTION_DURATION_MS   4000
